<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        <mapper namespace="cn.bd.dailyReport.dao.RoleDao">
            <select id="getByRoleValue" parameterType="String" resultType="Role">
                select * from role WHERE roleValue = #{roleValue}
            </select>
        </mapper>